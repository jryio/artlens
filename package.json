{
  "name": "artlens",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "watch": "open index.html && bun build src/index.ts --watch --outfile=index.js --target=browser"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
